import { NullCheckInterceptor } from './null-check.interceptor';

describe('NullCheckInterceptor', () => {
  it('should be defined', () => {
    expect(new NullCheckInterceptor()).toBeDefined();
  });
});
